{
  "framework": "nextjs",
  "buildCommand": "npx prisma db push && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "github": {
    "silent": true
  }
}